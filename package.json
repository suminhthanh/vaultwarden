{
  "name": "vaultwarden-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir build --env ''",
    "dev": "wrangler dev --env ''",
    "test:e2e": "vitest run --config vitest.config.ts",
    "test:e2e:watch": "vitest --config vitest.config.ts"
  },
  "devDependencies": {
    "vitest": "~3.2.0",
    "wait-on": "^8.0.1",
    "wrangler": "^4.84.1"
  },
  "dependencies": {
    "@types/ws": "^8.18.1",
    "ws": "^8.21.0"
  }
}
